{
    "realm": "${env.SSO_REALM}",
    "auth-server-url": "${env.SSO_SERVER_URL}",
    "ssl-required": "external",
    "resource": "${env.SSO_RESOURCE}",
    "verify-token-audience": true,
    "credentials": {
      "secret": "${env.SSO_SECRET}"
    },
    "confidential-port": 0
}